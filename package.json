{
  "name": "node-header-parser",
  "version": "1.0.0",
  "description": "A Node JS service that outputs user client information",
  "main": "lib/index.js",
  "scripts": {
    "build": "babel src --out-dir lib --source-maps inline --copy-files",
    "start": "babel-node src -d lib",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "es6",
    "babel",
    "fcc",
    "header parser",
    "node",
    "user agent"
  ],
  "author": "DavidMorais",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.4",
    "express": "^4.15.4"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.25.0",
    "babel-preset-es2015": "^6.24.1"
  }
}
